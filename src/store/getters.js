// 用户信息
export const userInfo = (state) => state.userInfo;
//搜索历史
export const searchHistory = (state) => state.searchHistory;
//个人歌单
export const playListMine = (state) => state.playListMine;
//收藏歌单
export const playListCollect = (state) => state.playListCollect;
//保存cookie
export const cookie = (state) => state.cookie;
// export const currentactive = (state) => state.currentactive;
// 当前播放url
export const currenturl = (state) => state.currenturl;
//当前歌曲详情
export const songDetails = (state) => state.songDetails;
//当前歌曲列表
export const playsonglist = (state) => state.playsonglist;
//下一首播放列表
export const nextsonglist = (state) => state.nextsonglist;
// 当前播放状态
export const playstatus = (state) => state.playstatus;
//当前播放顺序
export const playorder = (state) => state.playorder;
//当前音量
export const volume = (state) => state.volume;
//当前播放时间
export const currentTime = (state) => state.currentTime;
